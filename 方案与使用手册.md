# AI Coding Flow · 完整方案与使用手册

> 一套**个人**使用的 AI Coding 方案，适用于任何 git 仓库，**Claude Code 和 Codex 两端通用**。
> 参考：货拉拉技术《个人提效，攒不成组织提效：货拉拉 AI Coding 落地实践》（2026-09-16）。
> 版本：v1.1（2026-09-20，新增代码知识库 `.ai/kb/` 与 `/flow-kb`）。44 个自动化测试全部通过；验证环境为 Claude Code 2.1.232 和 codex-cli 0.155.0。

本文分成两部分，可以单独阅读：

- **上篇 · 方案**：为什么这样设计，由哪些部分组成，每个环节怎么运作，怎么度量。
- **下篇 · 使用手册**：怎么安装，日常怎么用，一个需求从头到尾怎么走，出了问题怎么办。

分章节的细节文档在 `docs/` 目录下。本文是它们的合订版，两者内容一致。

---

# 上篇 · 方案

## 1. 一句话概括

**简单任务直接做；复杂任务走「提案 → 方案 → 拆任务 → 逐任务实现 → 独立审查 → 回写」六步。**

- 每一步都留下档案，可以度量。
- 审查由另一个独立的模型来做，可以跨厂商：Claude 写、Codex 审，或者反过来。
- 踩过的坑写回 spec 库，下一个需求会自动召回。

## 2. 要解决的问题

原文的核心判断是：**个人提效攒不成组织提效**。工具推开之后，交付效率并没有明显提升。放到个人身上，同样的问题表现为这些：

| 现象 | 后果 |
|---|---|
| 每次都凭感觉决定让 AI 干多少 | 小活启动完整流程，又慢又烧 token；大活直接开写，写完大面积返工 |
| 项目知识只在自己脑子里 | 每次对话都要重新喂上下文，要么喂得太多，要么漏掉关键约束 |
| AI 写出「看起来很对」的代码 | 调用了不存在的 API、编了不存在的字段，测试不一定能测出来 |
| 做完就完，不复盘 | 同一类坑反复踩 |
| 说不清 AI 到底帮了多少 | 没法判断哪个环节该改进 |
| 同时用 Claude Code 和 Codex | 两边的规则、skill、MCP 各配一套，越来越不一致 |

## 3. 与原文的对应关系

原文是一个千人团队的三阶段演进。个人版保留其中的**方法**和**踩坑之后定下的硬规则**，组织层面的东西不做。

| 原文 | 个人版怎么落地 | 取舍 |
|---|---|---|
| 统一工作台 AiBox：统一管理 Rules / Skills / Commands / MCP | **个人工作台** `wb`：本仓库是唯一的配置来源，同步到 `~/.claude` 和 `~/.codex`，并检查两端是否不一致 | 不做下发和审计，只解决配置散落和两端漂移 |
| 阶段一 普及：培训、统一配置、高频场景示例 | **全局规则** + **5 个场景手册** + `/flow-onboard` 仓库接入 | — |
| 阶段二 规范驱动：SDD 五步 | **六步产线**，多出的第六步「回写」对应原文的第三阶段 | 需求来源从 PMIS 换成粘贴原文、issue 或文件 |
| spec 从四个库召回，打包成上下文清单，研发勾选 | tech / domain / dev / review 四类 spec，关键词加路径召回，**你来勾选** | 用关键词召回，不用向量库：规模小，而且能看到每条为什么被召回 |
| spec 用完打分，命中率低的修订，长期不命中的下架 | 审查放行后，逐条标注 hit / unused / mislead，`specs report` 给出修订或下架建议 | 常驻的检查项只按误导率考核 |
| 原子 delta，前后各校验一次，出错只回退一步 | 每个任务打 base / v1 / final 三个快照，可以只回退单个任务 | 快照存在隐藏的 git ref 里，不影响分支 |
| **坑一**：按任务步数分通道，阈值 2 | `/flow` 的硬标准：≤2 步、只改一个文件、验收一句话说得清，三条同时满足 | 多加了两条同样一眼就能判断的条件 |
| **坑二**：上下文要精准召回，不要全量塞 | 每条 spec 必须写明「什么条件下注入」，否则检查不通过；后续步骤只读勾选过的内容 | 照搬原文的准入门槛 |
| **坑三**：主从双 Agent，副 Agent 独立回源码核对 | 独立审查者**每轮新开**，只拿到 diff 范围；模型**每次由你选**，可以跨厂商 | 比原文多一层：同家族模型容易犯相同的错，跨厂商能再降一层 |
| 度量：AI 代码占比（三层签名）、trace、FPY | 原公式的 FPY、AI 代码占比的个人近似版、采纳率、审查独立级别 | 没有 IDE 遥测，但「只算最终留下来的行」这个关键点保留了 |
| 下一步：合入后的指标 | **已实现**：`aftercare` 统计 AI 代码存活率和后续改动 | — |
| 线上反哺：从报警回溯过程档案，把根因变成规则 | **已实现**：`/flow-incident` 加 `flowctl locate` | — |
| 组织形态、招聘标准、异步云端 Agent | 不做 | 与个人无关，或者原文自己也还在探索 |

## 4. 整体架构

```
┌──────────────────── 个人工作台（ai-coding-flow 仓库 = 唯一真相源）────────────────────┐
│  rules/  skills/  agents/  playbooks/  specs-starter/  workbench/mcp.json              │
│                         │  wb sync / wb doctor / wb mcp                                 │
│             ┌───────────┴────────────┐                                                  │
│             ▼                        ▼                                                  │
│      ~/.claude/…              ~/.codex/…             ~/.ai-flow/{bin,kit,specs}         │
└──────────────────────────────────────────────────────────────────────────────────────────┘
┌──────────────────── 宿主：Claude Code 或 Codex（任选，可中途切换）─────────────────────┐
│ ① 规则层  全局规则（受管区块，只在有 .ai/ 的仓库生效）+ 仓库 CLAUDE.md / AGENTS.md     │
│ ② 流程层  10 个 skill + 2 个角色（scout 召回上下文、verifier 独立审查）                 │
│ ③ 工具层  flowctl：状态 / 召回 / 打分 / 快照 / 度量 / 审查调度 / 合入后追踪 / 定位      │
│ ④ 资产层  spec 库（仓库 .ai/specs + 全局 ~/.ai-flow/specs）+ 变更档案 .ai/changes/     │
└──────────────────────────────────────────────────────────────────────────────────────────┘
          ▲ 复盘教训 / 线上问题 ─────── 回写成 spec ─────── 下一个需求自动召回 ┘
```

**七条设计原则**（每一条都来自原文的坑或经验）：

1. **通道按硬标准来分**：数步数，不凭感觉。
2. **上下文精准召回**：每条 spec 都要回答「什么条件下注入」。
3. **审查者必须独立**：上下文独立是底线，模型独立、厂商独立更好。
4. **人在环里**：提案和方案两步是硬门禁，必须人确认。
5. **一切从文件恢复**：状态都在 `.ai/` 里；换会话、换工具都能接着做。
6. **度量能逐行对账**：只相信能复算的数字，并写清楚每个指标的盲区。
7. **配置只有一个来源**：两端都从本仓库安装，不直接改 `~/.claude` 或 `~/.codex`。

## 5. 组成清单

### 5.1 十个 skill

| Skill | 何时用 | 做什么 | 你要介入 |
|---|---|---|---|
| `/flow` | 任何开发任务的入口，或者想知道「现在该干嘛」 | 判断通道，路由到下一步 | — |
| `/flow-onboard` | 第一次在某个仓库用 | 问 `.ai/` 要不要提交，调用 `/flow-kb` 生成代码知识库，再生成仓库规则文件和 3–8 条 spec 草稿 | ✋ 逐条确认 |
| `/flow-kb` | 接入时；代码漂移后 `flowctl status` 提示过期时 | `flowctl kb scan` 抽 Java/Go/Python 事实，规划页面，AI 逐页写作并标 `file:line`，lint、冻结基线；刷新只重写过期页 | ✋ 确认页面规划、抽查页面 |
| `/flow-propose` | 开始一个复杂需求 | 建变更，召回 spec 和代码入口，起草提案 | ✋ 勾选上下文、回答未决问题 |
| `/flow-design` | 提案通过后 | 写技术方案，每个引用都标到源码行 | ✋ 评审 |
| `/flow-tasks` | 方案通过后 | 拆成原子任务，每个任务都有可执行的验收 | 👀 过目 |
| `/flow-build` | 任务拆好后 | 逐个任务实现，每个任务打三个快照 | 👀 每个任务过目 |
| `/flow-review` | 全部任务完成后 | 每轮由你选审查模型，独立审查者回源码核对 | 选模型；连续 3 轮不过时介入 |
| `/flow-retro` | 审查放行后 | 算度量，把教训写成 spec，关闭变更 | ✋ 确认回写 |
| `/flow-incident` | 线上问题、测试提的 bug | 定位代码，追溯到当初的变更，找根因，生成规则 | ✋ 确认根因 |

### 5.2 两个角色

| 角色 | 职责 | 为什么独立出来 | 工具权限 |
|---|---|---|---|
| `flow-scout` | 读召回结果，定位代码入口，找相似的历史变更，返回带理由的候选清单 | 让主会话的上下文保持干净（坑二） | 只读 |
| `flow-verifier` | 核对 diff 里新引用的 API、字段、依赖是否真实存在，对照方案检查，跑验收 | 审查独立，不跟着实现者一起产生幻觉（坑三） | 只读，只跑测试 |

两个角色的说明各只有一份：`agents/*.md`。在 Claude 里它就是子 Agent 的定义；在 Codex 里，或者跨厂商调用时，flowctl 会把它转成 prompt。

### 5.3 五个场景手册（`playbooks/`）

读懂代码、新增字段和接口、修 bug、补单测、代码评审。每份手册都包含：走哪个通道、要准备什么、可以直接复制的提示词、期望的产出、怎么验收、常见翻车点，以及一个订单模块的例子。

### 5.4 两个命令行工具

- **`flowctl`**：流程工具，路径 `~/.ai-flow/bin/flowctl`。命令速查见下篇第 17 节。
- **`wb`**：工作台工具，路径 `~/.ai-flow/bin/wb`。

两个都只依赖 Python 3.9+ 标准库和 git。

## 6. 分通道

**走 Vibe 通道**，需要**同时**满足三条：

1. 步数 ≤ 2。一步指一处独立的改动加上对它的验证；阈值在 `.ai/config.json` 里，可以调整。
2. 只改一个文件。
3. 验收标准一句话说得清，没有需要澄清的地方。

走 Vibe 通道时：直接改，有对应的场景手册就照着做，改完跑一次测试就结束。不建变更，不召回 spec，不打快照。

**其余情况一律走 Spec 产线**。拿不准时也走产线，并说明是哪一条没满足。你明确指定通道时，按你说的办。

为什么要三条硬标准：原文的教训是，写成「简单任务用轻的」这种描述，人会凭手感选，最后要么全走重流程，要么全走轻流程。

| 例子 | 通道 | 理由 |
|---|---|---|
| 把某个接口的超时从 3s 改成 5s | Vibe | 1 步、1 个文件、验收清楚 |
| 修一个单函数里的空指针并补一个测试 | 产线 | 有 2 个文件（实现和测试） |
| 给订单加一个字段和对应的查询接口 | 产线 | 多文件、多步骤 |
| 「优化一下导出性能」 | 产线 | 验收说不清楚，需要先澄清 |
| 读懂订单模块 | 不占通道 | 不改代码，按 read-code 手册做 |

## 7. 六步产线

每一步都产出两样东西：一份**给人评审**的文件，以及**下一步 AI 要用的**信息。所有产物放在 `.ai/changes/<日期-slug>/` 下。

| 步 | 产物 | 给人看的 | 交给下一步 AI 的 | 门禁 |
|---|---|---|---|---|
| ① propose | `proposal.md`、`recalled.json` | 需求原文、意图、边界、验收标准（AC）、上下文清单、未决问题 | 意图、边界、AC、**勾选过的**上下文 | **硬**：勾选上下文，未决问题清零 |
| ② design | `design.md` | 方案、模块边界、接口契约、数据变更、Delta（新增 / 修改 / 删除） | 模块边界、接口约定、架构约束 | **硬**：评审 |
| ③ tasks | `tasks.md` | T1…Tn，每个都写了输入、输出、依赖、验收 | 单个任务的完整说明 | 轻 |
| ④ build | 代码 + `snapshots.json` | 每个任务的改动和验收结果 | 代码变更、快照 | 每个任务过验收并经你过目 |
| ⑤ review | `review.md`、`review/round-N.*` | 每轮的结论、发现（带证据）、引擎、模型、独立级别 | 放行结论、spec 打分 | 审查者给出 PASS |
| ⑥ retro | `retro.md`、`metrics.json` | 度量、教训和它落到哪里、spec 库的变动 | 下一轮的上下文基线 | 确认 |

**每一步的关键要求**：

- **propose**：需求原文照抄，不改写；原文没说清的写进「未决问题」，不自己补设定。召回结果先由 flowctl 写好，scout 只读不写。
- **design**：先读源码再写方案。每个类、方法、字段、表、配置都标 `file:line`，找不到的标「新增」。spec 和代码冲突时交给你决定。
- **tasks**：每个任务对应一条 Delta，大约是「AI 一次能写完、人几分钟能审完」的量。验收必须是一条能跑的命令。
- **build**：开工前打 base 快照；首版写完**立刻**打 v1 快照，在跑测试和修改之前；验收通过、你认可之后打 final 快照。同一个任务修 3 轮还不过就停下来。方案本身有错就回到 design，不在代码里绕过去。
- **review**：见第 8 节。
- **retro**：FPY 最低的任务必须找出原因；每条教训都要落到一个地方（新 spec、修订 spec、新审查项，或者写明不沉淀的理由）。

**贯穿全程的规矩**：一次只加载当前这一步的 skill；前面的细节从文件里读；每产出一版就记一次 `trace generated`，被采纳时记 `trace accepted`；产物里的 `<!-- flow:todo -->` 标记要等你确认后才删，flowctl 靠它判断进度。

**两条旁路**：

- **`/flow-onboard`**：仓库里没有 `.ai/` 时，`/flow` 会自动转到这里。
- **`/flow-incident`**：线上问题从这里进入。修复本身按分通道规则走，不在 incident 里做。

## 8. 独立审查与模型选择

### 8.1 独立性分三级

| 级别 | 做法 | 能防住什么 |
|---|---|---|
| L1 上下文独立 | 同一个模型，新开一个会话 | 被实现者的错误结论带偏（原文要求的底线） |
| L2 模型独立 | 同一家厂商、不同模型 | 同上，加上一部分模型特有的偏差 |
| L3 厂商独立 | Claude 写、Codex 审，或者反过来 | 同上，加上同一家族共有的知识盲区 |

### 8.2 每次由你选

每一轮审查、每一次召回上下文，skill 都会先执行 `flowctl models <角色>`，把两端可用的模型列给你选：

- 第一个选项是「和上次一样」，读取自 `~/.ai-flow/last_models.json`；
- 然后是另一家厂商的模型，标 L3；接着是同家族的其他模型，标 L2；最后是和实现者相同的模型，标 L1；
- Codex 的模型会附带说明和可选的推理档位，都从 `~/.codex/models_cache.json` 实时读取；
- CLI 不可用的一端不会列出来。

**选型建议**：审查者的能力不应低于实现者；scout 只做检索，用便宜的模型就够。

### 8.3 四种组合下实际发生什么

| 你在哪写代码 | 选的审查引擎 | 实际执行 |
|---|---|---|
| Claude | Claude | 原生子 Agent `flow-verifier`，模型用你选的 |
| Claude | Codex | `codex exec --ephemeral -s workspace-write -C <仓库> -m <模型> -c model_reasoning_effort=<档位> -o <输出>`，prompt 从标准输入传入 |
| Codex | Claude | `claude -p --agent flow-verifier --model <模型> --allowedTools Read,Grep,Glob,Bash` |
| Codex | Codex | 另起一个 `codex exec` 进程，天然是全新上下文 |

审查者拿到的**只有**：diff 的起点和终点、验收命令、它自己上一轮提的发现。**不包含**实现思路、结论或代码摘要，它必须自己回源码去查。

### 8.4 防止审查失真的四道保险

1. **结论必须明确**：输出里只有独占一行的「结论：PASS」或「结论：REJECT」才算数，而且只能出现一种。否则这一轮判为 INVALID。
2. **审查者不许改代码**：开审时把工作区存成一个 git ref，审完再比对。改了或删了已有文件，这一轮就作废；只是新增了文件（多半是测试缓存），会记一条警告。
3. **不能改写结论**：已经有结论的轮次不能重新记录，除非加 `--force`，而且表格里会注明「重新记录」。
4. **超时就终止**：外部审查者跑在单独的进程组里，超时连同它启动的测试进程一起结束。

### 8.5 实测结果

两次测试都用真实的 Codex（gpt-5.6-luna，推理档位 low）当审查者：

- **正常放行**：示例需求审查用时约 1 分钟。审查者自己跑了三条验证命令，每条结论都附了 `file:line` 证据，最后放行。
- **反向测试**：故意写了一段调用不存在方法 `repo.list_by_customer` 的代码。单元测试**全部通过**，因为没有测试覆盖到它；审查者回源码核对后判为「阻断 · 幻觉」，驳回。这正是坑三要解决的问题：逻辑读起来是通的，错的是事实。

## 9. spec 库

### 9.1 两层

| 层 | 位置 | 放什么 |
|---|---|---|
| 仓库级 | `<仓库>/.ai/specs/{tech,domain,dev,review}/` | 这个仓库特有的领域知识、架构约束、坑 |
| 全局 | `~/.ai-flow/specs/{…}` | 跨仓库通用的：语言和框架习惯、审查检查项 |

两层有同 id 的 spec 时，以仓库级为准。

### 9.2 格式与准入门槛

```yaml
---
id: domain-money-cents
type: domain                              # tech | domain | dev | review
triggers: [金额, amount, 导出, 报表]       # 需求文本命中任意一个就召回；英文按整词匹配
paths: ["orders/**"]                      # 改动路径命中也召回（** 可以跨目录）
when: 读写、展示或导出订单金额时            # 必填
status: active                            # draft | active | retired
---
# 一句话结论：写成可检查的断言，不要写「注意 xx」
## 为什么：踩过的坑、事故或 CR 意见的出处
```

`flowctl specs lint` 强制执行以下规则：

- 没有 `when` → **错误**；
- `triggers`、`paths`、`always` 三者都没有 → **错误**（永远召回不到）；
- 非 review 类设成常驻 → 警告；
- 触发词太短 → 警告；
- 正文太长 → 警告。

### 9.3 召回与生命周期

- **召回打分** = 命中的触发词数 + 2 × 命中的路径数。用过 3 次以上的 spec，还会按历史命中率加权。常驻的 review 检查项不参与召回，由审查者每次必查。
- **打分**：审查放行后，对每条召回的 spec 标注 `hit`（用上了且有帮助）、`unused`（召回了但没用）或 `mislead`（把实现带偏了）。
- **`specs report` 的处理建议**：
  - 误导率 ≥ 20% → 修订；
  - 命中率 < 30% → 收紧触发词；
  - 连续 10 次没命中 → 建议下架。

  阈值都在 config.json 里。下架就是把 status 改成 `retired`，文件保留。

## 10. 度量

原文的原则：具体数值不可比，有价值的是**算法能逐行对账**。

| 指标 | 算法 | 盲区 |
|---|---|---|
| **FPY**（首次正确率） | (终版行数 − 被改行数) ÷ 终版行数，按任务计算，按行数加权 | 衡量的是生成质量，不是代码质量；FPY 高也可能只是人懒得改 |
| **丢弃率** | 首版中被删掉的行 ÷ 首版行数 | 补 FPY 的盲区：AI 多写、后来被删掉的部分 |
| **AI 代码占比** | 终版新增行中，能和各任务 AI 首版逐行对上的比例（按次数匹配，`}` 这类行不会被重复计算） | Vibe 通道的改动不计入；手写的行和 AI 写的恰好相同时，会被算成 AI 的 |
| **采纳率** | 各步骤的采纳次数 ÷ 生成次数 | 依赖 skill 如实记录 |
| **审查独立级别** | 每轮记录 L1、L2 或 L3 | 样本少时没有参考价值 |
| **存活率**（aftercare） | 合入 N 天后，当初 AI 写的行还剩多少 | — |
| **后续改动数**（aftercare） | 关闭变更之后，又有多少次提交改了这批文件 | 后续改动不一定是返工 |

**快照时机决定了 FPY 准不准**：v1 必须在首版写完**立刻**打，在跑测试、自我修正、人工修改之前。AI 看到测试失败后自己改的部分，同样算「被改」，因为 FPY 衡量的就是「第一次就写对」的能力。

**squash 合入**：执行 `metrics --to <squash 提交> --squash`，只统计这一个提交。直接用主干作为终点，会把别人的提交也算进来。

**示例档案的实际数值**：T1 首版把金额直接按分输出，验收失败后改了 1 行，FPY 为 96%；T2 一次写对，FPY 为 100%；按行加权后 FPY 为 97%，AI 代码占比 30/31 = 97%。

## 11. 闭环

```
需求 ─→ 产线 ─→ retro：教训 → spec / 审查项 ──┐
                   │                          │
                   └→ 合入 2–4 周后 aftercare  ├─→ spec 库 ─→ 下一个需求自动召回
线上问题 ─→ incident：locate → 当初的变更档案 → 哪一环漏了 → 根因 → 规则 ──┘
```

`/flow-incident` 会对照当初那次变更的 proposal、design、tasks、review，从下面几项里判断是哪一环漏了：需求没澄清、上下文没召回、spec 缺失或错误、方案缺陷、验收不够、审查漏检（连同当时的引擎、模型和独立级别一起记录），或者这次改动根本没走流程。

## 12. 个人工作台

| 配置 | 真相源 | Claude Code 落点 | Codex 落点 | 同步方式 |
|---|---|---|---|---|
| 全局规则 | `rules/global.md` | `~/.claude/CLAUDE.md` 的受管区块 | `~/.codex/AGENTS.md` 的受管区块 | 只改标记之间的内容，改前备份，保留原文件的换行格式 |
| Skills | `skills/*` | `~/.claude/skills/` 软链接 | `~/.codex/skills/` 软链接 | 同名不覆盖，报冲突 |
| Agents | `agents/*.md` | `~/.claude/agents/` 软链接 | 没有原生对应，由 flowctl 转成 prompt | — |
| 命令入口 | — | `~/.ai-flow/bin/{flowctl,wb}`、`~/.ai-flow/kit` | 同左 | 软链接 |
| MCP | `workbench/mcp.json`（已加入 .gitignore） | `claude mcp add-json -s user` | `codex mcp add` | **默认只显示差异**，逐条确认后才写入 |
| 模型 | 没有配置文件，每次问你 | — | — | — |

**MCP 同步的安全规则**：

- 导入清单时，env 和 headers 这类字段的值会被替换成 `<from-host>`，不写进文件；
- 两端有同名但配置不同的 server 时，只管理先出现的那一端，并给出提示；
- 覆盖会丢字段的，只提示你手动处理；
- 配置不一致的，默认不覆盖，要加 `--allow-update` 才会更新；
- Codex 不支持 sse 传输，这类 server 只报告，不硬写；
- 清单之外的 server 永远不动。

**治理在个人版里对应什么**：准入 = 只有进了本仓库的配置才会被同步；合规 = `wb doctor` 检查漂移；审计留痕 = 变更档案里的 trace；用量统计 = 直接用两端自带的统计，不重复造；效能 = `flowctl report`。

## 13. 已验证与未验证

| 项目 | 状态 |
|---|---|
| flowctl、wb 的全部功能 | ✅ 44 个自动化测试，全部在临时目录中运行，用假的 CLI 替身 |
| 文档和 skill 里引用的命令、参数、链接 | ✅ 由一致性测试自动检查 |
| Claude 写、Codex 审（放行和驳回各一次） | ✅ 真实模型 |
| 两端都能识别 10 个 skill | ✅ Codex 实测；Claude Code 当前会话已加载 |
| 安装到本机、体检 | ✅ `wb doctor` 显示一切正常；规则文件里原有的内容未被改动 |
| 模型按 skill 说明完整执行一个真实需求 | ⏳ 还没做，需要你在真实仓库里跑一次 |
| 在 Codex 里写代码、由 Claude 审查（`claude -p`） | ⏳ 只用替身测过 |
| Codex 沙箱是否会拦截写 `.git` 的命令 | ⏳ 推断需要申请提升权限，未实测 |

## 14. 演进路线与风险

| 阶段 | 内容 | 什么时候做 |
|---|---|---|
| v1（当前） | 以上全部 | — |
| v1.1 | 跑满 5 个真实需求后，按 `flowctl report` 调整步数阈值、召回数量、修订阈值；清理示例 spec | 有数据之后 |
| v2 | 线上问题和监控系统自动关联；返工率按「同一批行被再次修改」精确计算 | 需要更准的合入后指标时 |
| v3 | 异步通道：tasks 里互不依赖的任务并行交给 worktree 里的子 Agent，逐个审查 | 单任务 FPY 稳定之后 |

| 风险 | 对策 |
|---|---|
| 流程太重，自己都懒得走 | 分通道是第一道闸，硬门禁只有两个 |
| spec 库变成垃圾堆 | 准入检查 + 打分 + 下架；retro 必须处理 report 里的标记 |
| v1 快照打晚了，FPY 失真 | 写成 build 步骤里固定的一步；`status` 会显示每个任务缺哪个快照 |
| 审查者被实现者带偏 | prompt 由 flowctl 生成，skill 规定不许夹带内容；每轮新开；工作区比对 |
| 召回漏掉关键 spec | 提案阶段的人工勾选兜底；retro 时把漏召回写成新的触发词 |

---

# 下篇 · 使用手册

## 15. 安装（一次）

```bash
cd ~/Documents/personal/ai-coding/ai-coding-flow
make test        # 44 个测试，全部在临时目录运行，不碰真实配置
make doctor      # 体检：两端 CLI、链接、规则区块、命令入口、spec 库、MCP
make sync-dry    # 预览会改哪些东西
make sync        # 安装（已于 2026-09-18 执行过）
```

`make sync` 做的事：

- 把 10 个 skill 软链接到两端；
- 把 2 个角色链接到 Claude 端；
- 在两端的全局规则文件里写入受管区块，改前备份到 `~/.ai-flow/backups/<时间>/`；
- 创建 `~/.ai-flow/bin/flowctl`、`~/.ai-flow/bin/wb` 和 `~/.ai-flow/kit`；
- `~/.ai-flow/specs` 不存在时，用示例 spec 初始化。

重复执行是安全的。修改本仓库里的 skill 会立即生效，因为装的是软链接；只有新增 skill 目录或改了 `rules/global.md` 时，才需要重新执行 `make sync`。日常更新用一条命令：`make update`（`git pull --ff-only` + `make sync` + `make doctor`）。

**MCP 同步（可选，单独一步）**：

```bash
python3 workbench/wb.py mcp import          # 用两端当前的配置生成 workbench/mcp.json（敏感值会脱敏）
# 编辑每个 server 的 "hosts"，写上希望它出现在哪几端
python3 workbench/wb.py mcp diff            # + 缺失  ~ 不一致  ! 需手动或不支持  · 清单外
python3 workbench/wb.py mcp apply           # 逐条确认；加 --allow-update 才会覆盖不一致的配置
```

## 16. 日常使用

### 16.1 场景速查

| 场景 | 对 Claude Code 或 Codex 说 | 会发生什么 |
|---|---|---|
| 第一次在某个仓库用 | `/flow-onboard` | 问 `.ai/` 要不要提交 → 探索代码 → 生成规则文件和 spec 草稿 → 等你确认 |
| 改个配置、修一个单文件小 bug | `/flow 把导出接口超时改成 5s` | 判为 Vibe → 直接改 → 跑测试 → 结束 |
| 不熟悉的代码 | 按 `playbooks/read-code.md` 的模板提问 | 输出分析报告，不改代码 |
| 一个跨文件的需求 | `/flow` + 需求原文，或者需求文件路径 | 判为产线 → propose → **停下等你勾选** |
| 继续昨天的需求（可以换个工具） | `/flow` | 读取 `flowctl status`，接着做下一步 |
| 线上报警、测试提的 bug | `/flow-incident` + 报错信息 | 建档 → 定位 → 追溯 → 根因 → 规则 → 转去修复 |
| 提交前自己过一遍 | 按 `playbooks/code-review.md` | 结构化 CR 报告 |
| 每周复盘 | 见 16.4 | — |

### 16.2 一个需求完整走一遍

下面以 `examples/export-orders/` 的「订单批量导出 CSV」为例。这份档案是真实跑出来的。

**① propose**：你说 `/flow 运营要按状态批量导出订单 CSV 给财务对账，字段含金额（元）`。

- AI 判断：多文件，走产线。执行 `flowctl new export-orders`，把需求原文贴进 proposal.md。
- AI 问你 scout 用哪个模型。你选一个，比如便宜的 sonnet 或 luna。
- 召回结果：`domain-money-cents`（因为命中了「金额」「导出」两个触发词，以及路径 `orders/**`）。
- scout 返回代码入口：`orders/repo.py:10 list_by_status`、`orders/model.py:5 Order`……
- **你要做的**：在上下文清单里勾掉无关的、补上漏掉的，回答未决问题，确认验收标准 AC-1 到 AC-4。然后说「可以」。

**② design**：AI 只读你勾选过的内容，写出 design.md。

- 方案是新增 `orders/export.py:to_csv`，再加 `OrderService.export_csv`。
- 每个引用都标了行号；Delta 分为新增和修改两类。
- **你要做的**：评审方案。有意见就提，AI 改完再给你看，直到你说「可以」。

**③ tasks**：拆成两个任务。

- T1：`to_csv` 加单测，验收命令 `python3 -m unittest tests.test_export`；
- T2：`export_csv` 加集成用例。
- **你要做的**：过一眼。

**④ build**：逐个任务实现。

- T1：打 base 快照 → AI 写首版 → **立刻**打 v1 快照 → 跑验收，失败了（金额直接输出了分）→ 按 spec 修正 → 通过 → 你过目 → 打 final 快照 → 提交。
- T2：同样的流程，一次写对。

**⑤ review**：AI 问你这一轮用哪个审查者。

- 你选 Codex / gpt-5.6-luna / low，属于跨厂商（L3）。
- flowctl 启动审查者，结论追加到 review.md：

```
| 轮 | 结论 | 引擎 / 模型 | 独立级别 | 输出 | 备注 |
| 1 | PASS | codex / gpt-5.6-luna / low | L3 | review/round-1.out.md |  |
```

- 放行后，AI 给召回的 spec 打分：`domain-money-cents` → hit。

**⑥ retro**：`flowctl metrics` 的输出：

```
任务        v1行    终版行    被改行    丢弃行    FPY    丢弃率
T1         25     25      1      1    96%     4%
T2          6      6      0      0   100%     0%
按行加权 FPY：97%
AI 代码占比（到 HEAD）：30/31 = 97%
```

- 归因：T1 的 spec 已经召回并勾选了，但实现时没有遵守，问题不是「缺 spec」。
- 回写：新增一个审查项 `review-checked-domain-specs`，要求审查时逐条核对勾选过的 domain spec 是否被遵守。
- **你要做的**：确认回写内容。AI 执行 `flowctl close`，并提醒你 2–4 周后跑一次 aftercare。

**你总共要介入的时间**：提案约 5 分钟，方案约 10 分钟，任务约 1 分钟，每个任务 1–2 分钟，复盘约 3 分钟。

想自己重新跑一遍这个例子：

```bash
make demo                                          # 审查用替身，不花 token；结果在 examples/.demo-fake/
make demo-codex MODEL=gpt-5.6-luna REASONING=low   # 审查用真实 Codex；会覆盖 examples/export-orders/
```

### 16.3 线上问题

你说 `/flow-incident 导出 CSV 金额差 100 倍` + 报错或截图。AI 会依次：

1. `flowctl incident new export-amount-x100`，建立问题档案；
2. 复现，找到出问题的代码位置；
3. `flowctl locate orders/export.py:13`，找到引入这一行的变更档案；
4. 对照当时的档案，勾出是哪一环漏了，写出根因；
5. 回写：新 spec 的触发词要能匹配**同一类需求**，不只是这一次问题的描述；
6. 等你确认后，转去修复：小修复走 Vibe，否则新建一个变更并链接这个问题档案。

### 16.4 每周复盘（约 10 分钟）

```bash
F="$HOME/.ai-flow/bin/flowctl"
"$F" report             # 各变更的 FPY、AI 占比、采纳/生成、审查轮次和级别；看趋势
"$F" specs report       # 处理标着「修订」或「下架」的 spec
"$HOME/.ai-flow/bin/wb" doctor     # 两端有没有漂移
```

对合入满 2–4 周的变更，再执行 `"$F" aftercare <变更>`，查看存活率和后续改动。

### 16.5 换工具继续

流程状态全部在 `.ai/` 里。在 Claude Code 里做到 build 一半，打开 Codex 说 `/flow`，它会读 `flowctl status` 接着做下一步。反过来也一样。

## 17. 命令速查

### flowctl（`"$HOME/.ai-flow/bin/flowctl" <子命令>`）

| 类别 | 命令 | 说明 |
|---|---|---|
| 生命周期 | `init [--local-only]` | 初始化 `.ai/`；`--local-only` 会把它加进 `.git/info/exclude` |
| | `new <slug> [--title]` / `use <变更>` / `list` / `status` / `close` | 新建、切换、列出、查看进度、关闭变更 |
| 留痕 | `trace <stage> <event> [--task] [--note]` | 记录 generated、accepted、rejected 等事件 |
| spec | `recall [--text] [--file] [--paths] [--record] [--json]` | 召回 spec |
| | `score <id> hit\|unused\|mislead [--note]` | 给 spec 打分 |
| | `specs lint` / `specs report` | 准入检查 / 修订与下架建议 |
| 知识库 | `kb scan [--json]` / `kb plan [--json]` | 抽取代码事实到 `.ai/kb/scan.json` / 规划页面到 `plan.json` |
| | `kb draft --all\|--page <id> [--force]` / `kb facts --page <id>` | 套模板出草稿 / 给出该页的事实清单 |
| | `kb lint` / `kb freeze` / `kb status [--json]` | 校验引用 / 生成索引并记基线 / 漂移检测（0 新鲜、10 过期、11 无基线） |
| | `kb recall [--text] [--file] [--paths] [--top] [--json]` | 召回知识库页 |
| 快照 | `snapshot T<n> base\|v1\|final` | 任务快照，存为隐藏 ref |
| | `checkpoint` | 把工作区存成审查用的 diff 终点 |
| 角色 | `models scout\|verifier [--json]` | 列出可选的引擎和模型 |
| | `agent scout --host --engine --model [--reasoning] --input` | 按你的选择运行 scout |
| 审查 | `verify start --host --host-model --engine --model [--reasoning] [--timeout]` | 开一轮审查 |
| | `verify record [--round] [--force]` | 记录原生子 Agent 的审查结果 |
| 度量 | `metrics [--to <sha>] [--squash] [--json]` / `report` | 单个变更的度量 / 所有变更的趋势 |
| 闭环 | `aftercare [变更] [--at]` | 合入后的存活率和后续改动 |
| | `locate <文件>[:行]` | 这一行来自哪个变更档案 |
| | `incident new <slug> [--title]` | 建立问题档案 |

### wb（`"$HOME/.ai-flow/bin/wb" <子命令>`，或者在仓库里用 `make`）

| 命令 | 说明 |
|---|---|
| `sync [--host claude\|codex\|all] [--no-rules] [--dry-run]` | 安装或更新 |
| `doctor` | 体检，有问题时退出码不为 0 |
| `diff` | 两端的差异：skill、规则区块、MCP |
| `mcp import [--force]` / `mcp diff` / `mcp apply [--host] [--yes] [--allow-update]` | MCP 清单：生成 / 对比 / 写入 |
| `uninstall [--host]` | 只移除它自己装的东西；`~/.ai-flow` 保留 |

## 18. 目录与文件

```
ai-coding-flow/                       本仓库，唯一真相源
├── 方案与使用手册.md                  本文
├── README.md  Makefile  install.sh  PLAN.md
├── docs/                             分章节的详细文档（00–10 + 附录）
├── rules/global.md                   全局规则（写进两端的受管区块）
├── rules/project-template.md         仓库规则文件的模板
├── playbooks/                        五个场景手册
├── skills/                           10 个 skill；flow/scripts/{flowctl.py,kb.py}；flow/templates/（含 kb/ 7 个页模板）
├── agents/                           flow-scout.md、flow-verifier.md
├── workbench/wb.py                   工作台
├── specs-starter/                    全局 spec 的初始内容
├── examples/                         demo.sh、export-orders/（真实档案）、src/
└── tests/                            44 个测试 + 假的 codex、claude CLI

~/.ai-flow/
├── bin/flowctl、bin/wb、kit          指回本仓库的软链接
├── specs/                            全局 spec 库
├── last_models.json                  上次选的模型
└── backups/                          规则文件改动前的备份

<任意仓库>/.ai/
├── config.json  current
├── specs/{tech,domain,dev,review}/  specs/_scores.jsonl  specs/_template.md
├── kb/                              代码知识库：README.md ai-quick-reference.md architecture/（10 页）
│                                    modules/<模块>.md domains/<域>/{README,核心流程,术语梳理,配置清单}.md
│                                    scan.json plan.json _manifest.json
├── changes/<日期-slug>/
│   ├── proposal.md design.md tasks.md review.md retro.md
│   ├── recalled.json snapshots.json trace.jsonl metrics.json aftercare.json
│   └── review/round-N.{json,prompt.md,out.md}
└── incidents/<日期-slug>.md
git refs：refs/flow/<变更>/<任务>/{base,v1,final}、refs/flow/<变更>/review/<N>
```

## 19. 常见问题

**1. `.ai/` 该不该提交？**
`/flow-onboard` 会问你。个人项目建议提交，spec 和档案都是资产。公司仓库不方便提交的话，用 `init --local-only`，它只留在本地。

**2. 审查判 INVALID 怎么办？**
只有两个原因。一是审查者改动了已有文件：用 `git status` 和 `git diff` 看改了什么，恢复后重开一轮。二是输出里没有唯一一行结论：换个模型或者提高推理档位，再开一轮。已经有结论的轮次不能重新记录，要重新审查就开新一轮。

**3. 审查只报了「新增文件」的警告？**
多半是测试缓存（`.pyc`、`coverage` 等）。结论仍然有效。把这些文件删掉，或者加进 `.gitignore`。

**4. v1 快照忘了打？**
这个任务的 FPY 会失真：`metrics` 会显示「快照不全」，或者数值偏高。补不回来，就在 retro 里注明。下个任务记得在首版写完后立刻打。

**5. 任务要回退？**
修改过的文件：`git restore --source=refs/flow/<变更>/T<n>/base -- <文件>`；这个任务新建的文件直接删除。用 `git diff --name-status refs/flow/<变更>/T<n>/base` 可以区分两者。执行前先确认文件列表。

**6. 快照 ref 会被推到远端吗？怎么清理？**
不会，`git push` 默认只推分支和标签。清理某个变更的快照：
`git for-each-ref --format='%(refname)' refs/flow/<变更>/ | xargs -n1 git update-ref -d`

**7. Codex CLI 不可用？**
`make doctor` 会报出来。重装：`npm install -g @openai/codex@latest`。修好之前，选模型时 Codex 不会出现在选项里，可以先选 Claude 的其他模型（L2）。

**8. 在 Codex 里执行 snapshot 或 verify 提示没有权限？**
这几个命令要写 `.git`，verify 和 agent 还要启动另一个 CLI（需要联网）。在 Codex 里申请提升权限后重试，不要跳过。

**9. squash 合入后度量怎么算？**
`flowctl metrics --to <squash 提交> --squash`。不要用主干作为终点。

**10. 手动改了规则区块会怎样？**
`wb doctor` 会显示 `edited`，下次 `sync` 会被覆盖。要改规则，请改 `rules/global.md`，然后执行 `make sync`。如果标记被破坏，状态会显示 `damaged`，sync 会拒绝修改这个文件，需要你手动修复。

**11. `wb sync` 报冲突？**
说明目标位置已经有同名的文件或目录，不是本仓库装的。它不会被覆盖，你自己决定改名或者删除。

**12. 两端各放了一份相同 skill 的拷贝？**
`wb diff` 会列出来。想统一管理的话，把它移进本仓库的 `skills/`，删掉两端的拷贝，然后执行 `make sync`。

**13. MCP 里的 token 会进仓库吗？**
不会。导入时，env 和 headers 的值都会被替换成 `<from-host>`；参数或 URL 里像是凭据的会给出提示；`workbench/mcp.json` 已经加入 `.gitignore`。

**14. 想调整步数阈值或召回数量？**
编辑仓库的 `.ai/config.json`：`lane_step_threshold`、`recall_top`，以及修订和下架的各项阈值。

**15. 怎么卸载？**
`make uninstall`：移除两端的链接和规则区块，其余内容原样保留。`~/.ai-flow` 需要的话手动删除。

## 20. 附：原文要点速记

- **核心判断**：个人提效攒不成组织提效；工具推开之后，交付效率并没有跟着提升。
- **三个阶段**：普及（统一工作台 AiBox、培训、高频场景），规范驱动（SDD 五步，参考 OpenSpec 的 delta 结构和 Superpowers 的 Skill 化），AI Native（资产沉淀和线上反哺）。
- **自研的四件事**：需求系统接入、spec 自动召回、用完打分回写、全链路 trace。
- **三个坑**：
  - 简单任务不启动完整流程，按步数分通道；
  - 上下文不是越多越好，要精准召回，并设准入门槛；
  - 幻觉要靠第二只眼睛，副 Agent 必须独立回源码核对。
- **度量**：AI 代码占比（三层签名，只算进入主干的行）、trace（生成次数与采纳次数之差）、FPY（按任务计算、按行加权，有两处容易误读）；下一步要补合入后的指标。
- **人机分工三个层级**：L1 全人工；L2 人定目标、AI 起草、人做决审（原文所处的位置，也是本方案的位置）；L3 AI 自己闭环、人兜底抽查。卡住的不只是模型能力，还有交付稳定性和决策权的划分。
