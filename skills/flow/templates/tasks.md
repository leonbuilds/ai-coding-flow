# {{title}} · 任务拆解

<!-- flow:todo -->
> 变更：{{change}}
> 每个任务是一个原子 delta：单一目的、能独立验证、失败时只回退这一步。粒度大约是 AI 一次生成、人几分钟能审完。
> 格式固定为 `- [ ] T<n> 标题`，flowctl 靠它统计进度。写完删掉 flow:todo 标记。

- [ ] T1 
  - 输入：<!-- 依赖的文件、接口、前置任务产物 -->
  - 输出：<!-- 会新增 / 修改哪些文件、符号 -->
  - 依赖：无
  - 验收：<!-- 一条能跑的命令，或一条具体可检查的断言 -->

- [ ] T2 
  - 输入：
  - 输出：
  - 依赖：T1
  - 验收：
