---
id: {{id}}
type: kb
kind: overview
summary: <!-- kb:todo -->
triggers: {{triggers}}
paths: {{paths}}
sources: {{sources}}
updated: {{date}}
protected: false
---

# 项目概览

<!-- 写作规则：每条论断后面写 file:line 或 file:起-止；scan 事实里没有、你也没亲自打开确认的，一律写「待确认」。代码块不超过 10 行，知识库只放引用不放实现。图用 mermaid。人工补充放在 <!-- kb:manual --> … <!-- /kb:manual --> 之间，重新生成时会按同名标题原位保留。frontmatter 只改 summary，其余由 flowctl kb draft 维护。 -->

<!-- 规划目标：{{goal}}；scan 提示：{{hints}} -->

## 一句话

<!-- 这是什么系统、给谁用。 -->

## 技术栈与版本

<!-- 只写依赖清单里能读到的，版本以 manifest 为准。 -->

| 组件 | 版本 | 出处 |
|---|---|---|
| <语言/框架/中间件客户端> | <版本> | <manifest 文件:行> |

## 构建、测试、lint 命令

<!-- scan 给的命令都是猜测。你必须实际跑过一次再标「已验证」，跑不了就标「猜测，未跑」并写原因。区分跑全部测试和只跑某一块，产线 verifier 优先用后者。 -->

```bash
# 构建            （已验证 / 猜测，未跑）
<命令>
# 跑全部测试
<命令>
# 只跑某个模块 / 包
<命令 <占位符>>
# lint / 格式检查
<命令>
```

## 目录地图

<!-- 只列 AI 会去改或必须先理解的目录，说职责不列文件；每行给出对应的模块页。 -->

| 目录 | 职责 | 模块页 |
|---|---|---|
| <path/> | <这个目录负责什么> | [module-x](modules/x.md) |

## 运行与配置文件

<!-- 只列路径，不要把配置值抄进来（可能含密钥）。 -->

- <配置文件路径> — <作用>

## 引用文件

<!-- 本页每一处 file:line 都要在这里出现一次；格式 `- path:line — 作用`。flowctl kb lint 会校验路径存在、行号不超范围。 -->

- <!-- kb:todo -->
