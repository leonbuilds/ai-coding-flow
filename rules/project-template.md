# <仓库名> · AI 协作规则

<!-- 填写说明：这份文件只写 AI 从代码里读不出来的东西。API 签名、目录结构这些 AI 自己 grep 就能看到的，不要抄进来；抄进来只会在代码变了之后变成一份说谎的文档。由 /flow-onboard 首次生成草稿，之后随代码演进人工维护。 -->

## 项目概览

<!-- 填写说明：一句话说清这是什么项目、给谁用；再列技术栈（语言、框架、关键中间件版本）。例如："订单管理服务，供内部客服和结算系统调用；Go + gRPC + MySQL + Kafka。" -->

## 目录地图

<!-- 填写说明：只列 AI 会去改动或需要先理解的目录，别抄完整目录树。每行说清"这个目录是干什么的"，而不是"这个目录里有什么文件"。 -->

| 目录 | 职责 |
|---|---|
| <path> | <这个目录负责什么，AI 改动时要注意什么> |

## 构建与测试命令

<!-- 填写说明：精确到能直接复制执行的命令，不要写"用 make test 之类的命令"这种含糊说法。区分"跑全部测试"和"只跑某一块"，产线的 verifier 会优先用后者。 -->

```bash
# 构建
<精确命令>

# 跑全部测试
<精确命令>

# 只跑某个模块/包的测试
<精确命令 <占位符>>

# lint / 格式检查
<精确命令>
```

## 编码约定

<!-- 填写说明：只写这个项目和通用习惯不一样的地方。比如"这里不用官方推荐的 xx 库，因为 yy"，或者"错误处理统一走 pkg/errcode，不要用 fmt.Errorf 拼字符串"。通用的编码规范（命名、格式化）不用写，交给 linter。 -->

- <约定 1，附上为什么这么定>
- <约定 2>

## 禁区

<!-- 填写说明：列出不能碰、或改动前必须额外确认的文件、接口、数据。每条写清原因（下游依赖、合规要求、历史坑），而不是只列名字。 -->

| 对象 | 为什么不能随便动 |
|---|---|
| <文件/接口/表名> | <原因> |

## 领域术语

<!-- 填写说明：项目里特有的黑话、缩写、和字面意思不一致的命名。每条给一句准确定义，方便 AI 读代码时对上号，不要抄字典释义。 -->

| 术语 | 含义 |
|---|---|
| <术语> | <在本项目里的准确含义> |

## 与 spec 库的关系

<!-- 填写说明：说明本仓库的 spec 存放位置、和全局 spec 的关系；不用重复解释 spec 机制本身（那是 flow 工具链的事）。 -->

- 本仓库的 spec 在 `.ai/specs/`，产线各步骤按 `flowctl recall` 的结果自动召回，不需要在这里手工罗列。
- 新发现的坑、评审意见，走 `/flow-retro` 或 `/flow-incident` 回写成 spec，不要直接堆进本文件。
